<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Stock Manager Advance</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"> <!-- Font Awesome for icons -->
    <style>
        /* Global Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }

        body {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden; /* Prevents scrolling */
            background-color: #f4f7fb; /* Light background color */
        }

        /* Main container for the page layout */
        .main-container {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
            height: 100%;
            position: relative;
        }

        /* Left side container for background image */
        .image-container {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url("https://media.istockphoto.com/id/1311598658/photo/businessman-trading-online-stock-market-on-teblet-screen-digital-investment-concept.jpg?s=1024x1024&w=is&k=20&c=JZprgGDQ8xqa6iu0fyKJfKOlAvae0w9U-AdHeCT2kg4=");
            background-size: cover;
            background-position: center;
            z-index: -1; /* Ensure the image is behind the form */
        }

        /* Right side container for login form */
        .form-container {
            background-color: rgba(255, 255, 255, 0.9); /* Slight transparency for effect */
            padding: 40px;
            box-shadow: 0 15px 25px rgba(0, 0, 0, 0.3); /* Increased shadow for floating effect */
            width: 100%;
            max-width: 400px; /* Smaller width for the login form */
            display: flex;
            flex-direction: column;
            justify-content: center;
            border-radius: 12px;
        }

        /* Heading */
        .main-heading {
            text-align: center;
            font-size: 36px;
            font-weight: bold;
            color: #ffbb33; /* Gold color for heading */
            margin-bottom: 30px;
        }

        /* Form container styling */
        h2 {
            text-align: center;
            color: #333;
            font-size: 26px;
            margin-bottom: 20px;
        }

        /* Form fields */
        .form-group {
            margin-bottom: 15px;
        }

        label {
            display: block;
            font-weight: bold;
            margin-bottom: 5px;
            color: #333;
        }

        input[type="text"],
        input[type="password"] {
            width: 100%;
            padding: 12px;
            border-radius: 5px;
            border: 1px solid #ccc;
            font-size: 16px;
            margin-top: 5px;
            box-sizing: border-box;
        }

        input[type="text"]:focus,
        input[type="password"]:focus {
            border-color: #ff6a00; /* Focus color change */
            outline: none;
        }

        /* Submit button styling */
        .submit-btn {
            width: 100%;
            padding: 14px;
            background-color: #ff6a00;
            color: #fff;
            font-size: 18px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .submit-btn:hover {
            background-color: #ffbb33; /* Hover effect */
        }

        /* Links styling */
        p {
            text-align: center;
            font-size: 14px;
            color: #1b1a19;
        }

        p a {
            color: #ff6a00;
            text-decoration: none;
            font-weight: bold;
        }

        p a:hover {
            text-decoration: underline;
        }

        /* Responsive Design */
        @media screen and (max-width: 768px) {
            .main-container {
                flex-direction: column;
            }

            .form-container {
                padding: 30px;
                max-width: 350px;
            }
        }
    </style>
</head>

<body>

    <div class="main-container">
        <div class="image-container"></div>

        <div class="form-container">
            <h1 class="main-heading">Welcome to Stock Manager Advance</h1>
            <h2>Login</h2>
            <form action="/login" method="post">
                <div class="form-group">
                    <label for="name">Username:</label>
                    <input type="text" id="name" name="username" placeholder="Enter your username" required autocomplete="off">
                </div>
                <div class="form-group">
                    <label for="password">Password:</label>
                    <input type="password" id="password" name="password" placeholder="Enter your password" required>
                </div>
                <button type="submit" class="submit-btn">Login</button>
                <p>Don't have an account? <a href="/signup">Signup</a></p>
               
            </form>
        </div>
    </div>

</body>

</html>
